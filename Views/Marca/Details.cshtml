@model AppEcommerce.Models.Marca

@{
    ViewData["Title"] = "Details";
    Layout = "_LayoutAdmin";
}

<div class="row justify-content-center">
    <div class="col-md-6 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Detalhes</h4>
                <p class="card-description"> Detalhes sobre a Marca </p>
                <form class="forms-sample" asp-action="Create" id="form" enctype="multipart/form-data" method="post">
                    <input type="hidden" asp-for="Id" />
                    <div class="form-group">
                        <label class="control-label">Marca</label>
                        <input asp-for="Nome" class="form-control" placeholder="Marca..."  readonly/>
                        <span asp-validation-for="Nome" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <div class="col-12 mt-2 text-center">
                            <img src="@Model.Imagem" class="img-fluid" id="PreviewImage" />
                        </div>
                    </div>
                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary mr-2">Editar</a>
                    <a asp-action="Index" class="btn btn-light">Voltar</a>
                </form>
            </div>
        </div>
    </div>
</div>