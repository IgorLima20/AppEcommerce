@model AppEcommerce.Models.Marca

@{
    ViewData["Title"] = "Cadastro";
    Layout = "_LayoutAdmin";
}

@* <h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Marca</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Imagem)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Imagem)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div> *@


<div class="row justify-content-center">
    <div class="card text-center">
        <div class="card-header">
            Excluir
        </div>
        <div class="card-body">
            <h5 class="card-title">Deseja Realmente Excluir essa Marca ?</h5>
            <p class="card-text">Confirme a Exclusão dessa Marca !!</p>
            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="Excluir" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-light">Cancelar</a>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script type="text/javascript">
    let ok = '@Html.Raw(ViewBag.Concluido)';
    if (ok == 'OK')
    {
        Swal.fire({
            icon: "success",
            title: "Marca Excluida com Sucesso!!",
            type: "success",
            confirmButtonColor: "#3085d6",
            confirmButtonText: "OK",
            closeOnConfirm: true,
            allowOutsideClick: false,
        }).then((result) => {
        if (result.isConfirmed) {
            window.location.href = '@Url.Action("Index", "Marca")';
        }
    })
    }
</script>
}