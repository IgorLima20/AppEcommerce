@model AppEcommerce.Models.Imagem

@{
    ViewData["Title"] = "Delete";
    Layout = "_LayoutAdmin";
}

@* <h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Imagem</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Img)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Img)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="IdImagem" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div> *@


<div class="row justify-content-center">
    <div class="card text-center">
        <div class="card-header">
            Excluir
        </div>
        <div class="card-body">
            <h5 class="card-title">Deseja Realmente Excluir essa Imagem ?</h5>
            <p class="card-text">Confirme a Exclusão da Imagem !!</p>
            <form asp-action="Delete">
                <input type="hidden" asp-for="IdImagem" />
                <input type="submit" value="Excluir" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-light">Cancelar</a>
            </form>
        </div>
    </div>
</div>
